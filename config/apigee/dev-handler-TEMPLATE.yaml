# The apigee CLI creates a handler file including the key and the secret merge
# merge this into your setup that might include further configurations: 
# https://docs.apigee.com/api-platform/istio-adapter/operation#configuring_the_adapter
# Store this file in a save location and NOT in this repository.
#
# istio handler configuration for apigee adapter
# generated by apigee-istio provision on 2018-09-04 19:26:00
apiVersion: config.istio.io/v1alpha2
kind: apigee
metadata:
  name: apigee-handler
  namespace: istio-system
spec:
  apigee_base: {from the cli}
  customer_base: {from the cli}
  org_name: {from the cli}
  env_name: {from the cli}
  key: {from the cli}
  secret: {from the cli}
  api_key_claim: {optional:the attribute to take the api key from if you are using a custom jwt}

